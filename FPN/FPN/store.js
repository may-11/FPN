$(function() {
 
  //セレクトボックスが切り替わったら発動
  $('select').change(function() {
 
    //選択したvalue値を変数に格納
    var val = $(this).val();
 
    //if文でバリューを変数に変えて１、その変数に合った地図を読み込む２。
      
      if()
});